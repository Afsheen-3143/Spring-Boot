spring.application.name=user

# MySQL example - change username/password/db name as needed
spring.datasource.url=jdbc:mysql://localhost:3306/batchinguser
spring.datasource.username=root
spring.datasource.password=Afsheen@3143
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update

# Ensure the batch schema is created
spring.batch.jdbc.initialize-schema=always

# Logging for debugging
logging.level.org.springframework.batch=INFO
logging.level.com.batchprocessing=DEBUG
